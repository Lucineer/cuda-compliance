[package]
name = "cuda-compliance"
version = "0.1.0"
edition = "2021"
description = "EU AI Act compliance engine — automated policy checking, risk assessment, and audit trails for agent fleets"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
